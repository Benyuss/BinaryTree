package binarytree;

class BinaryTreeTraversed<T> implements Traversal<T> {

	@Override
	public void traverseTree(Node<T> current) {
	}

	/* private String bitSet;
	private Node root;
	
	public BinaryTreeTraversed (String bits) {
		bitSet = bits;
	}

	@Override
	public void traverseTree(Node<T> current) {
	}
	
	public void travWay(String bits) {
		BinaryTree.Travers trav = traversal();
		
		switch (trav) {
			case PREFIX: {
				prefix(bits);
			}
			case INFIX: {
				infix(bits);
			}
			case POSTFIX: {
				postfix(bits);
			}
		}
	}
*/
}